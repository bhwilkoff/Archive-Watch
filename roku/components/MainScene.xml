<?xml version="1.0" encoding="utf-8" ?>
<!-- The shell. ROKU-DESIGN §2: an Overhang, a LEFT rail, and a content column.
     Settings is not a surface (§2.3) and Surprise is not a surface (§2.4). -->
<component name="MainScene" extends="Scene">
  <interface>
    <field id="deepLinkContentId" type="string" />
  </interface>
  <script type="text/brightscript" uri="pkg:/components/Theme.brs" />
  <script type="text/brightscript" uri="pkg:/components/MainScene.brs" />
  <children>
    <Rectangle id="bg" width="1920" height="1080" color="0x0B0B0CFF" />

    <!-- §4.4 Overhang, 115px. The (*) indicator is the platform's own "there
         are options here" affordance and using it is the cheapest way to look
         native (§5.7). -->
    <Rectangle id="overhang" width="1920" height="115" color="0x16161AFF" />
    <Label id="brand" />
    <Label id="clock" />
    <Label id="optHint" />

    <Group id="content" />
    <NavRail id="rail" />

    <Label id="loading" />
  </children>
</component>
