<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- The Studio's encrypted sign-in tokens: their Keystore key never
         leaves the device, so a restored copy could not be decrypted. -->
    <exclude domain="sharedpref" path="studio_oauth.xml"/>
</full-backup-content>
