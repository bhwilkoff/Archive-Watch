# Watch Together — launch audit (2026-09-23)

Owner: *"Can you audit all features, design, and code within the Watch Together
Studio and the Watch Together implementations across all platforms to see if
anything can be improved, optimized, or better built out for launch once the
YouTube oauth approval is available?"*

Five read-only reviews (macOS UI, shared engine, iOS/tvOS, Android + web +
Worker + Roku, launch readiness), merged and ranked here. **This is the work
list for the Studio loop**: take items from the top, prove each on the product
path (Decision 130/133), and strike it with the version that fixed it.
Items marked *(suspected)* were inferred from code and need a run before a fix.

## A. Launch blockers

| # | Item | Where | Status |
|---|---|---|---|
| A1 | Preview → Go Live deleted the broadcast it had just created (end() after arming) | macOS `goLive()` | **FIXED v1.42.536**, proven on YouTube (RDmqaI1PgVw live → complete), §8.54 |
| A2 | App Store builds carried no YouTube/Twitch client id — sign-in "not set up" on every store build, and Google's reviewer is told to install from the store | `appstore-build.yml`, `play-release.yml` | **FIXED v1.42.537** — `AW_YOUTUBE_CLIENT_ID` / `AW_TWITCH_CLIENT_ID` secrets, build fails if missing. Needs a store build before the verification reply goes out |
| A3 | YouTube chat captured one access token for the whole show — dies within the hour, then polls 401s | `StudioSession.attachYouTubeChatIfArmed` | **FIXED v1.42.537** (token asked for per page) — not yet run past an hour |
| A4 | YouTube quota: `liveChatMessages.list` (5 units) polled all show on a project-wide 10,000/day budget — about one 2-hour show a day for ALL hosts; no `quotaExceeded` handling | `StudioChatYouTube.swift`, `StudioPlatforms.swift` | **Decision 136** (owner chose all three: slim sign-in + quota extension + own stream key). **Partly FIXED v1.42.539**: chat floor 10 s, viewer count 60 s, exponential backoff, quotaExceeded stops and says so (§8.56). Stream-key path BUILT on macOS v1.42.540 (Sign in / Stream key; no API, no quota) — not yet proven with a real key; iOS/tvOS/Android still sign-in only. Chat opt-in DONE v1.42.547: "Show chat from YouTube" on the Mac, iPhone and Apple TV go-live forms, off by default and remembered; live check ESeqclB1yg8 read no chat with it off. **OWNER**: file the YouTube API quota extension |
| A5 | YouTube API Services policy: privacy page must link YouTube ToS + Google Privacy Policy and say the app uses YouTube API Services; a ToS line at sign-in | `privacy.html`, `StudioSignInRow` | **FIXED v1.42.541** — YouTube API Services section with the ToS and Google Privacy Policy; a ToS line under Sign in to YouTube on every Apple platform |
| A6 | privacy.html contradicts itself/the code: "collects nothing"/"only server-side thing" vs rooms; sign-out now also revokes; uninstall does not remove Keychain items; Twitch uses (title/category PATCH, markers, viewer count) undisclosed; "Apple TV app" intro | `privacy.html` | **FIXED v1.42.541**, and the six-hour promise made true by the Worker's hourly sweep (deployed 37ea0138) |
| A7 | Docs/demo say the app "transitions the broadcast to live"; code uses `enableAutoStart`. Readiness does a `liveBroadcasts.list?mine=true` not disclosed | reply, privacy, demo beat 8 | **FIXED v1.42.541** |
| A8 | Demo step 2 revokes at myaccount.google.com — shown not to work for Brand Accounts | `tools/oauth_demo_record.sh`, Desktop checklist | CODE: "Sign out in the app (it revokes), confirm the scope prints in words" |
| A9 | Which OAuth clients live in the Google project? The social poster's upload client may share it; the reply says `auth/youtube` is the only scope | Cloud Console | **RESOLVED 2026-09-23**: project 895559137709 has ONE OAuth client, "Archive Watch (Apple)" (iOS); the social poster's client is elsewhere, so "the only scope" is true. Branding has no Terms of Service URL — deliberately NOT added while Data Access is under review: saving branding "will update your verification request" and may re-review verified information. Add `https://archivewatch.org/terms.html` after the review concludes |
| A10 | One keystroke ends a live show with no confirmation: ⌘W on the Studio, Esc on the projection ✕, ⇧⌘E from any window, End; ⌘Q never completes the broadcast | macOS | **FIXED v1.42.545** (§D37): close disabled on air, confirm on ⇧⌘E / End / ✕ / ⌘Q, ⌘Q completes the broadcast; bench-verified `closable=no` while live |
| A11 | Film can change under a live show: projection window autoplays the next film (macOS); Play Next / auto-advance and lineup/channel entry (tvOS); engine follows any item swap without a rights re-check | `PlayerWindow_macOS`, `DetailView.swift`, `StudioEngine.noteFilmItemChanged` | **FIXED v1.42.546** — every advance site refuses while live and Play Next leaves the tvOS menu (§8.57, control fails 4/4). Open: a rights re-check inside the engine as defense in depth |
| A12 | tvOS End cancels its own teardown task — the YouTube broadcast is neither completed nor deleted (orphans); Menu mid-show ends it silently | `DetailView.swift` | **Orphan half FIXED v1.42.538** — `completeArmedBroadcast` runs outside the caller's cancellation; §8.55 (a real request: wrapped reaches, bare refused -999). Not yet run on an Apple TV. Menu half built v1.42.551 (Rule 8.8i: cover not dismissable while live, Menu asks) — needs a real remote press |
| A13 | iOS End leaves the camera and mic running (privacy dots, battery) | `StudioPlayerContainer_iOS.end()` | **FIXED v1.42.542** — `StudioSession.stopHostCapture()` on End and on disappear; iPhone 12 bench run: `AWCAM host capture stopped running=no` 97 ms after the show ended |
| A14 | Worker: ~~no stale-room sweep~~ (**hourly sweep deployed v1.42.541**, SQL tested on the real schema); ~~no rate limit~~ **FIXED v1.42.552** (deployed bf4cae7e): per-address limiter 300/min (measured: engages, ~860 through across two bursts, Cloudflare's count is approximate) + 10 creates/min; filmID/position/rate validated; a host update without filmID keeps the film; presence capped at 50; /together answers its own preflight (* , max-age 86400) | `worker/` | done |
| A15 | Android has no foreground service for a live broadcast — locking the phone kills camera/mic | Android manifest | **BUILT v1.42.553** — `StudioBroadcastService` (camera\|microphone, "You're live" notification with End). Pixel 8a: service started types=192, and with the screen OFF (dozing) the camera count kept rising 342→805 in 25 s at 19–23 fps. 2026-09-23 evening, app switched to Settings (not the screen): camera 139→628 in 25 s at 19–22 fps with the service, and the system turned the screen off at 19:49:12 during it (power_screen_state reason 2, not a harness key) without stopping the camera. CONTROL still not run — and now explained: both control launches happened while the phone was ASLEEP (yesterday my own screen-off; today a system screen timeout at 19:49:12, reason 2, despite AC + stay-on), so the activity was paused (`wm_pause_activity … sleep`) and no show could start. Not a code fault; the next run checks Awake + unlocked first |
| A16 | Rooms: a pending code joins whatever film plays next (Android, iOS, tvOS); followers never leave on close (web, iOS, tvOS) — inflated presence, next film hijacked | followers, `watch.js` | **Apple FIXED v1.42.548** — code tied to the room's film, iOS/tvOS leave on close (§8.58, control fails). Android and web FIXED v1.42.550 (Android code tied to the film; web leaves on close and on navigation) |

## B. Should-fix before or soon after launch

- **Engine**: media packets handed over through one `Task` each — REPLACED v1.42.570 with one ordered stream per track; a DEBUG counter (AWORDER) measured 0 frames out of order on BOTH the new code and the old per-task code over 60 s on the Mac bench, so reordering is NOT shown to be the audio-glitch cause; the counter stays to catch it on a phone or TV; ~~A/V timestamps from counters drift over two hours *(suspected)*~~ (v1.42.598's "not reproduced" is WITHDRAWN: that 1,200 s run broadcast the "Thanks for watching" CARD the whole time — 88 kbps, no film audio — so the audio clock it meant to test never ran. RE-MEASURED v1.42.600 with the film on air — 511 MB, film audio present: audio-minus-video spans read −0.348, −0.332, −0.348, −0.331, then −0.325 s at the end (1200.33 video / 1200.01 audio), i.e. a CONSTANT equal to the start offset the engine logged (`AWCLOCKS firstAudio-firstVideo=-333 ms`), varying < 25 ms over 20 min. **No drift on macOS** — CAVEAT found afterwards: the bench's local-monitor mute also silences the film in the broadcast on macOS (the tap is inside the player), and the owner's FaceTime camera and microphone were attached, so the audio measured was most likely the ROOM MICROPHONE, not the film. It still shows a hardware audio clock holding against the host clock for 20 min; the film-tap path is unmeasured. Recordings deleted. OPEN, and separate: that start offset suggests audio is stamped ~0.29 s EARLY relative to the picture (the first audio sample arrived 333 ms after the first frame but carries pts 0.046) — the same direction as the earlier "macOS audio leads by ~130–150 ms" note. Needs a flash-and-beep clip to measure lip sync properly; not changed on inference. The clip and a DEBUG `AW_PLAY_URL` route exist (v1.42.601 attempt), but the measurement needs the film UNMUTED — i.e. beeps from the owner's speakers — which waits for the owner's yes.) **And the card was a real defect**, FIXED v1.42.599: the selected scene persists, so a Studio last left on "Thanks" went live with the closing card over the next show; a show now never opens on a closing-card scene (falls back to the first cardless scene, "Film"). Re-run: `AWSCENE opened on Film rather than the closing card`, 3.6 Mbps, film audio mean −43 dB / peak −12 dB. ~~token refresh has no single-flight~~ (FIXED v1.42.565: StudioRefreshGate; §8.59 — 8 racers, 1 refresh, control 7 refused); ~~`encodeAndAwaitFormat` can hang go-live forever~~ (FIXED v1.42.569: a refusal or 5 s of silence fails the start; §8.60); ~~`blankFrame()` force-unwrap + unbounded pixel-buffer pool~~ (FIXED v1.42.571: 12-buffer ceiling, last frame held, no force-unwrap; §8.61); ~~reconnect callbacks from a cancelled connection can fail the new one~~ (FIXED v1.42.572: close news carries its connection; §8.62 — 5 back-to-back reconnects, 0 stale reports; the old publisher: 5); ~~simulcast extras never reconnect~~ (FIXED v1.42.567: same backoff, never ends the show; bench: extra severed via a relay → 4 failed attempts during the outage → reconnected on the 5th, primary reconnects=0; the first version stopped retrying after 2 — the control); ~~`prepare()` leaves a broadcast behind on a failed bind~~ (FIXED v1.42.566, proven on YouTube: forced bind failure → broadcast 1rOHKyQwsek deleted, Upcoming clean); a new reusable `liveStream` every show is KEPT as a trade-off — reusing or deleting one needs liveStreams.list/delete (not declared in the quota request or privacy page) or storing a stream key (the privacy page promises never); ~~server `onStatus` text may echo the key *(suspected)*~~ (FIXED v1.42.597 on Apple and Android: every server-written sentence passes `redactingKey(in:key:)` / `redactKey` before the screen or a log, including the percent-encoded form; Android had been printing the server's RAW AMF reply on a refusal. §8.68 5/5 with its control; Kotlin 3/3. Not provoked against a real ingest).
- **DEBUG doors reachable in Release** on every Apple platform (`AW_STUDIO_DEST` can send a rehearsal to any server; `applyLaunchOverrides`, StudioLab, `AW_STUDIO_CHAT`, `AW_GOLIVE_CUSTOM` …). Wrap all in `#if DEBUG`; decide whether "Custom server" is a product feature (OBS has it) or DEBUG-only.
- **macOS**: ~~go-live `problem` invisible once on air and raw `\(error)`~~ (FIXED v1.42.557: shown in the on-air summary; shared `studioSentence(for:)`); ~~room code has two sources of truth~~ (FIXED v1.42.557: `RoomJoin.hostCode` reads `StudioRoomHost.code`); ~~landing page's "Start a room…" is always disabled and contradicts §11.13~~ (FIXED v1.42.588: removed, with the untrue "Open a film to go live." under it; the Studio's "Friends on your call" now also offers **Copy invite link** — `https://archivewatch.org/#/together/<code>-<film>`, the browser route verified working in v1.42.584 — so a friend with no app joins from the call's own chat); ~~scene switch can UNMUTE the mic~~ (FIXED v1.42.556, self-test 10/10, control fails 2); ~~⇧⌘S collides with Save As; ⌘1–9 from any window~~ (FIXED v1.42.560: ⇧⌘S removed, ⇧⌘L opens the Studio; scene keys only while the Studio is key, via focusedSceneValue); accessibility: FIRST PASS v1.42.558 (scene buttons named + selected trait, faders/meters/mute switches named with values, audience rows combined with a named "Show on the broadcast" action) — not yet heard with VoiceOver; guest-window menu now fills without hover (only where Screen Recording is already granted) and text fields carry their titles, v1.42.559; ~~keyboard tile framing still open~~ (BUILT v1.42.608: the tile in STREAM is focusable — arrows move it 1% of the frame, Shift 10%, Option-arrows reshape it; clamped exactly as a drag; labeled for VoiceOver with the keys as its hint. The clamp is now `StudioCameraFraming.nudged` in shared code and §8.69 proves it — move, edge stop, bottom stop, widen-and-pull-back, minimum, and a control showing the raw move leaves the frame. The key presses themselves are not exercised: no synthesized input on the owner's Mac); whole window re-evaluates every second (PARTLY FIXED v1.42.562: chat, shout-out, camera fps, sending rate and the audio warnings are assigned only when changed; `health` still changes every tick — splitting the columns into their own views is the remaining half; CLOSED ON MEASUREMENT v1.42.617: an 8 s `sample` of a live muted bench show — app 40-48% CPU — found the main thread 90.7% idle, all SwiftUI graph updates ~6% of it and `StudioWindowView`'s body 1.9%. The cost is decode and encode off the main thread, so the split would buy nothing measurable and is not done); ~~`measuredKbps` assumes 30 fps~~ (FIXED v1.42.591); ~~no "Going live…" progress~~ (FIXED v1.42.580: the button shows a spinner and "Going Live…" while the platform calls run); ~~recording clock breaks past an hour~~ (FIXED v1.42.591: the LIVE badge's H:MM:SS).
- **iOS/iPad**: Duck toggle never reaches the engine alone (Decision 133); leaving the app kills the picture (auto-PiP, no scenePhase handling) — **BUILT v1.42.554**: no PiP in the Studio; Intermission goes up when the app stops being active and comes down on return (iPhone 12: `AWAWAY` up/down logged and the show published cleanly afterwards; measured DURING a live show v1.42.555: on the old code a 15-s trip to Settings killed the VIDEO for the rest of the show (VideoToolbox session invalidated, -12903, nothing rebuilt it; audio kept flowing). The engine now rebuilds a dead encoder — proved by a forced invalidation on the Mac bench: 2 fps → STOPPED → restarted → 33 fps within a second, 0 reconnects. A second iPhone on-air trip did not kill the encoder at all, so iOS does not do it every time); ~~iPad controls sheet covers the program~~ (BUILT v1.42.594 under new IPAD-DESIGN §5b: `.inspector` — a column beside the program at regular width, the same sheet on iPhone; closed through its binding rather than `dismiss`; no iPad on the bench, so not yet seen); ~~denied camera/mic is a dead end (no Open Settings)~~ (BUILT v1.42.576: the refusal is named, with Open Settings unless restricted — not yet seen on a device); ~~Twitch activation URL is plain text~~ (FIXED v1.42.593: a link on iOS and macOS — the URI already carries the code — and on the ANDROID PHONE, which was worse: it showed a QR and "scan the code … on your phone" to someone holding the phone; a QR is now only drawn on a television; built, not yet seen on a device); ~~an ended show is titled "Could not go live"~~ (FIXED v1.42.577: "The broadcast ended", "This film cannot be played" or "Could not go live" per cause, and a refused start is `studioSentence(for:)` rather than a raw error); ~~kbps is a lifetime average (tvOS too)~~ (FIXED v1.42.591 on iOS, tvOS and macOS: `encodedKbpsRecent`, a ~5 s window from the engine's once-a-second sample; §8.67 — an hour at 4,000 then a collapse to 500 reads 500, the lifetime average still reads 3,942); ~~no §D21 stall sentence (tvOS too)~~ (FIXED v1.42.613: iPhone and Apple TV call the shared `StudioFilmStall.reason` after three silent seconds — no item, buffering, an error, or "playing but no frames reaching the program" — and say it in the capsule, the sheet and the ten-foot readout; a host's pause is excluded, per v1.42.592; built, not yet seen on a device); ~~host pause reads as a fault on tvOS~~ (FIXED v1.42.592 on tvOS AND iOS — the phone's capsule and sheet had the same line: a host's pause (`timeControlStatus == .paused`) no longer reads "the film has stopped"; buffering still does, and the camera, audio and connection lines are untouched; built, not yet seen on a device); ~~Back from GoLiveTV leaves the film paused~~ (FIXED v1.42.595: Menu dismisses a full-screen cover without `onCancel`; `onDismiss` now resumes a film that was PLAYING when Go Live paused it and did not go live; built, not run — the bedroom TV was not woken at night). The iPad inspector (§5b) could not be looked at either: the iPad Pro reported `Locked` at launch.
- **Android**: Go-live dialog still says camera is optional and enables Go live without it (Decision 132); ~~denial has no retry/Open Settings~~ (BUILT v1.42.576: a refusal is named with Open Settings, and the grant is re-read on return — compiled, not yet seen on the Pixel); room status never shown to a guest; TV join grid double focus targets; ~~encrypted prefs may break after device transfer~~ (FIXED v1.42.607: `studio_oauth.xml` excluded from cloud backup AND device transfer — `allowBackup="false"` does not stop Android 12+ transfer, and both rules files were still placeholder comments; and an undecryptable store is now deleted and recreated instead of failing every open forever, which would have left that host unable to sign in on the new phone. Compiled; not yet provoked on the Pixel); ~~sign-out does not revoke~~ — **FIXED v1.42.575**: the token is cleared on the tap and revoked at Twitch off the composition scope; Twitch's revoke takes an ACCESS token and refuses an expired one, so a stale token is renewed (unsaved) first — Apple had been sending the refresh token, fixed in the same change. Request shape read from Twitch: our client id + a bogus token → `token Invalid token`; no client id → `missing client id`.
- **Sync**: correction policy differs — "paused" means buffering on Swift/Android but not web; only Swift seeks with `seekLead` (WEB DONE v1.42.611: a seek aims ahead by this browser's own MEASURED seek time and nothing until one is measured — a default lead overshot in §8.66, +0.27 s at 0.5 and +0.2-0.26 at 0.2, against +0.05-0.12 with none; paused guests get no lead; §8.32 +3, old follower fails 3. ANDROID v1.42.612: the same rule — seek timed to STATE_READY, lead 0 until measured, none when paused; `seekTarget` unit-tested; not run on the Pixel, which is locked); ~~macOS host publishes buffering as a pause to every guest~~ (FIXED v1.42.596: the host publishes its INTENT (`rate`), not `timeControlStatus`, whose waiting state is buffering; and a host that falls behind after a stall now says so, because drift is measured against the room's clock since the last publish — the old check reset its baseline every half second and could never see a stall (§8.27 +6, its control). Mac bench, room KGWQ, muted: exactly three publishes over 75 s — create, host pause at 26.3, resume from 26.3; a real host stall was not provoked).
- **Worker**: validate `filmID`/`rate`/`position`; `/together` preflights answered by the global handler (web-TV presence fails); cap presence rows; `Access-Control-Max-Age`.
- **Twitch**: ~~hourly `/validate` during a show; drop unused `user:read:chat`~~ — **FIXED v1.42.574**: `token(for: .twitch)` validates on first use and hourly on every caller (Apple + Android), only a 401 clears the sign-in; the chat scope is no longer requested (§8.64 asks Twitch itself; an unreachable host is the control); ~~IRC reader drops split UTF-8 chunks and double-reconnects~~ — **FIXED v1.42.573**: bytes buffered and decoded per whole line, one ordered feed, one reconnect per failure of the CURRENT connection, old connection cancelled, `.waiting` reported (§8.63; the per-read decode fails 3 of its checks).
- **Recorder**: ~~non-fragmented MP4 loses everything on a crash; dropping P-frames corrupts~~ FIXED v1.42.568 — 10-s fragments (crash at ~35 s left a 30.0-s playable file; control without fragments: "moov atom not found"); a dropped frame now drops until the next keyframe.
- **Other**: ~~macOS mic usage string mentions only Creation Studio (5.1.1 risk)~~ (FIXED v1.42.577, read back from the built app's Info.plist); call-audio / window capture can carry copyrighted audio past the rights gate; ~~`selfDeclaredMadeForKids` hard-coded~~ (FIXED v1.42.606: no longer sent — it declared every broadcast "not made for kids" for the host, overriding a kids-audience channel; the field is optional on insert and YouTube then applies the channel's own audience setting. Not run against the API, which would create a broadcast. NB: YouTube disables live chat on made-for-kids broadcasts, so such a channel will show no audience chat — expected, not a fault); ~~revoked token still shows "Signed in"~~ (FIXED v1.42.578: a refresh the platform refuses as a dead grant — Google `400 invalid_grant`, Twitch `400 Invalid refresh token`, both read live — clears the stored token on Apple and Android; any other failure keeps it; §8.64, control fails 4); ~~`StudioRights` cutoff `<= 1929` should follow the year~~ (FIXED v1.42.579, Decision 137: the audit, the Studio and the social selector all compute it; 1,691 items from 1929-30 move presumed_pd -> safe_pd_age at the next build, keep-to-keep; the on-air line no longer says "before 1930"). Separately found: `tools/test_audit_rights.py` fails its `build_sqlite skip` case on HEAD too — not this change, not yet looked at.

- **Stream key on iOS** (Decision 136 says always offered; only macOS had it): BUILT v1.42.580, not yet seen on a device. tvOS and Android: not built (PARITY).

- **Web room join was broken since v1.42.469** — found v1.42.584 by opening a room in Chrome: `TogetherView` called a `summary` that js/api.js never exported, so every browser guest got "Watching … with the room" and no player. FIXED v1.42.584 (Detail's two sources: the catalog's `downloadURL`, then archive.org metadata); verified in Chrome against the live Worker — the player now OPENS with the film's source (the fix); a guest pause was undone within 1.2 s with the note shown; no rate saved. **CORRECTED v1.42.589**: the "landed at 257.8 s" reading is NOT evidence of playback — a tools-driven Chrome tab is hidden, Chrome defers media there (readyState 0), and the room's own seeks move `currentTime` anyway. A re-test on the LIVE site (v1.42.588, film SomethingAlwaysHappens) showed exactly that: position matching the room to 0.1 s at readyState 0. **SETTLED v1.42.590 by §8.66** (`tools/test_web_room_playback.mjs`: its own headless Chrome, muted, playback counted only at readyState ≥ 2 with the playhead buffered) against the LIVE site on two films — That Certain Thing: plays, guest 317.97 vs room 318.00, host pause lands on 318.16 exactly, ended room announced; SomethingAlwaysHappens (260 MB, index at the end): 315.23 vs 315.26, pause on 315.40 exactly. Control: the old join served locally fails 5; and an autoplay-blocked guest is told "Press play to join the room." §8.65 guards every `API.<name>` call; the old watch.js fails it.

- **A paused guest sat on a different frame from the host** — measured v1.42.585 on the Mac product path against a live room (new `AW_ROOM_JOIN` door on macOS, muted): host paused at 506 s, guest paused at 510.7 and stayed, because every correction returned none while paused. Swift, Kotlin and web now seek a paused guest to the host's paused position (no seek lead onto a still frame). Re-measured: host paused at 635, guest moved to 635.0 and held. The join itself also measured healthy: seek to 367.9, three 0.97 nudges, then in tolerance; a host jump to 1000 landed at 1005.6 two seconds later.

- **No Apple guest was ever told anything** — `StudioSyncFollower.status` was read by no view, so a failed join played the film alone as if in a room, an ended room went unmentioned, and a guest's own pause fought the room for up to 10 s (§11.6.1 promised a sentence). BUILT v1.42.586: `StudioRoomNotice` on macOS/iOS/tvOS, the guest's move answered at once. Measured on the Mac product path against a live room: guest pause → notice in 52 ms, playing again 160 ms later, cleared after 3 s; room ended → "The host ended the room." A window screenshot was not possible (the debug instance's window was not on this Space), so the evidence is the app's own log; iOS and tvOS are built, not run. Android BUILT v1.42.587 (same three sentences; a guest pause or seek wakes the poll at once; `isGuestMove` unit-tested 4/4; both flavors compile) — not run, the Pixel was dozing and locked.

- **Test-only launch settings in Release**: assessed v1.42.588 — 78 `AW_…` reads in Studio and Watch Together code are outside `#if DEBUG`. Each is an environment variable or a `defaults` key that only the person running the app can set, on their own machine; there is no remote path to any of them. Left as they are for now: wrapping 78 call sites is a wide edit whose risk is a Release build that no longer compiles, for hygiene rather than safety. Worth a single `StudioDoors.env(_:)` choke point that returns nil in Release when the Studio is next reorganized.

## C. Roku rooms (not built)

About 200–250 lines: an options row, a keyboard dialog, a `roomCode` on PlayerScreen driving `TogetherTask`, an ended/failed message — ~~plus three fixes in the existing BrightScript~~ (DONE v1.42.614: `awNowSeconds` is a Double and the two epoch fields are read from the JSON TEXT into Doubles, since a Float resolves 1.8e9 in 128 s steps; a failed poll is skipped and only 404/410 ends the room, via `DoesExist("ended")` — `room.ended = true` would have been a runtime type mismatch on every normal room; an anonymous "I'm here" every 30 s; and a paused guest is moved to the host's frame, the rule the other platforms gained today. Syntax-checked with bsc; NOT run on a Roku — nothing creates the task yet, and sideloading would switch on a television at night). The surface is BUILT v1.42.615 (ROKU-DESIGN §8a: Library ▸ Options ▸ Join, keyboard, one-shot lookup, film at the room's position, `PlayerScreen.roomCode` follows; failures in a StandardMessageDialog). Found while writing it: my v1.42.614 token line called a method on `CreateObject(...)`'s return value, a BrightScript compile error bsc did not flag — fixed. Whole-project bsc: 20 errors before and after (all pre-existing); design lint 0. NOT run on a Roku yet.

## D. Polish

Captions that explain rather than refuse/warn (macOS DONE v1.42.563; iOS, tvOS and Android DONE v1.42.564); typography beyond six levels and ~~hard-coded brand orange beside system-orange warnings~~ (FIXED v1.42.605: the Mac Studio's five hand-written marquee oranges — hex twice, RGB three times, all #FF5C35 — now read `Brand.primary` from a new macOS token file; warnings keep the SYSTEM orange on purpose, since brand color is chrome and never a fault signal); `StudioVoice*` code — KEPT on review (v1.42.605): SHAREPLAY §10 deliberately preserves `StudioVoiceCodec`/`StudioVoiceRoom` as transport-free parts for any future guest voice, with §8.19/§8.20 guarding them; only `StudioVoiceProbe` is inert (its measurement is finished and nothing launches it), 356 lines left for the owner's call rather than deleted against a design doc; ~~Android `createRoom/publish/endRoom` unused~~ (KEPT on review, v1.42.616: `StudioSyncClientLiveTest` uses them as the HOST that drives and checks the Android guest against the live Worker — test instrument, not dead code); ~~duplicated chat-attach in three places~~ (FIXED v1.42.616: `StudioSession.attachChat(to:)` — YouTube if armed, then the host's own Twitch channel, the DEBUG door last — called by macOS, iOS and tvOS; `test_studio_armed.sh` now follows helpers recursively, since a one-level follow read the extraction as a dropped chat id; its control still fails); ~~stale PARITY (rooms "not deployed")~~ (FIXED v1.42.603), SCRATCHPAD item 7, ~~`Secrets.xcconfig.example`~~ (FIXED v1.42.603: the TV client is optional and blank — a copied placeholder would have selected Google's device flow with a client that does not exist), ~~the `tv_signin_is_qr_and_phone` memory~~ (index line corrected); ~~web guest has native controls that fight sync~~ (FIXED v1.42.583: a guest's own pause or scrub is re-asserted at once with "The host controls the film." rather than undone up to 10 s later; the room's 0.97/1.03 nudges no longer save as the viewer's speed, which had made their NEXT films play 3% off; a room starts at the host's rate; §8.32 +7, the old follow fails 7); ~~iOS Watch Together two menus deep~~ (FIXED v1.42.604 under new iOS-DESIGN §3.5a: its own bordered menu in the Detail action row, one tap to *With friends…* / *With the world…*; built, not yet seen on the iPhone — Screen Time).

## Quota per two-hour YouTube show (review estimate — confirm against Google's table)

| Call | Units | Count | Total |
|---|---|---|---|
| insert stream + broadcast + bind | 50 each | 3 | 150 |
| thumbnails.set | 50 | 1 | 50 |
| complete / delete | 50 | 1 | 50 |
| viewer count every 30 s | 1 | 240 | 240 |
| chat list at 5 s | 5 | 1,440 | 7,200 |
| **Total** | | | **~7,700** of 10,000/day for the whole project |
