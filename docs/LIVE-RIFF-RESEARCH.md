# Live Riff Research — moved

This research is now **`docs/WATCH-TOGETHER-RESEARCH.md`**.

The feature set is **Watch Together** (with friends — SharePlay, Decision 098)
and **Watch Together Studio** (with the world — Decision 127). "Live riffing"
was the working name; this file was the last place it survived.

This pointer stays rather than the path simply disappearing, because
**Decision 127 cites `docs/LIVE-RIFF-RESEARCH.md` §1 and `DECISIONS.md` is
append-only** (Decision 092). A citation in an append-only file may not be
edited to match a later rename, so the path has to keep resolving.
