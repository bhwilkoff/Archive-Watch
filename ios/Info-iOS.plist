<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Background audio so Picture-in-Picture + AirPlay keep playing. -->
    <key>UIBackgroundModes</key>
    <array><string>audio</string></array>
    <!-- archivewatch://item/{id} custom scheme (Universal Links via /.well-known too). -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key><string>app.archivewatch.ios</string>
            <key>CFBundleURLSchemes</key><array><string>archivewatch</string></array>
        </dict>
    </array>
    <key>NSUserActivityTypes</key>
    <array><string>app.archivewatch.viewItem</string></array>
    <!-- Only HTTPS/TLS + Apple frameworks → export-compliance auto-answer. -->
    <key>ITSAppUsesNonExemptEncryption</key><false/>
    <key>UILaunchScreen</key><dict/>
</dict>
</plist>
