# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode
build/
DerivedData/
*.xcuserdata/
*.xcworkspace/xcuserdata/
*.moved-aside
*.pbxuser
*.perspectivev3
!default.pbxuser
!default.perspectivev3
*.mode1v3
*.mode2v3
!default.mode1v3
!default.mode2v3
xcshareddata/WorkspaceSettings.xcsettings

# Swift Package Manager
.build/
Packages/
Package.pins
Package.resolved

# iOS
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods (if ever used)
Pods/

# Fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# Node (shouldn't be needed, but just in case)
node_modules/

# Environment / secrets
.env
.env.local
*.key
credentials.json
Secrets.xcconfig
.claude/scheduled_tasks.lock
video_registry.db
__pycache__/
tools/validation_report.txt

# Prebuilt SQLite catalog — hosted via GitHub Releases, not committed (Decision 017)
catalog.sqlite
catalog.sqlite.gz
catalog.sqlite.zz
catalog-manifest.json

# Full catalog source — hosted on the catalog-source GitHub Release, not git
# (Decision 018). Pipeline fetch/publishes it via tools/catalog_release.py.
catalog.json
catalog.json.gz
ArchiveWatch/ArchiveWatch/catalog.json
metadata_review_batch.json
metadata_review_decisions.json
tools/.tmdb_synopsis_cache.json
tools/.tmdb_meta_cache.json

# Frame-extracted cover working set (#86) — durable copies live on archive.org
tools/covers_out/

# Swift build artifacts for the on-device cover scorer (#86)
tools/CoverScorerCLI/.build/

# Rights-audit manifest (generated; CI uploads it as an artifact, Decision 027)
tools/rejected_audit.csv
tools/rejected_*.csv

# iOS SPM build artifacts
ios/.build/
ios/.swiftpm/
/tmp/core-dd

# Xcode per-user state (never track — causes branch-switch/merge friction)
**/*.xcodeproj/xcuserdata/
**/*.xcodeproj/project.xcworkspace/xcuserdata/
tools/audit_series_episodes.csv
subs/
subs.tar.gz
